"""Unit tests for tinyDisplay components.

This package contains unit tests for individual components:
- Core components (ring buffer, database, expressions)
- Widget components (text, image, progress, shapes)
- Animation components (coordinator, timeline, primitives)
- Canvas components (canvas, composition, positioning)
- DSL components (parser, validator, generator)
- Rendering components (engine, frame timer, memory manager)
- Data components (sources, bindings, transactions)
"""
